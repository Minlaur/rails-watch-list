<div class="banner">
  <div class="container">
    <h1><span class = "yellow-highlight">🥸 WATCHAPPDOC is the only movie watchlist you need. </span> </h1>
    <p>A movie a day, who needs apples anyway? </p>
  </div>
</div>

<div class="container-item">
  <% @lists.each do |list| %>
    <div class="card">
      <%= cl_image_tag list.photo.key,:aspect_ratio=>"1:1", :effect=>"art:hokusai", :gravity=>"auto", :width=>200, :crop=>"fill"%>
      <%# <% it was previously creating an error because it could not detect the photo.key of the old database.
      i ran the rails db:drop, then create, migrate and now it works! for precaution, I added validates with photo too.%>
      <%= link_to list.name, list_path(list), class: "btn btn-primary" %>
    </div>
  <% end %>
</div>
<div class="vertical-center">
  <%= link_to "Add new list", new_list_path, class: "btn btn-ghost" %>
</div>
